# git-summary

# what this tool?
A CLI tool for visualizing Git repository status and changed files.

To install:

```bash
bun install @nazozokc/git-summary
```

This project was created using `bun init` in bun v1.3.11. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
